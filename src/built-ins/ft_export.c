#include "minishell.h"

int	ft_export(t_cmd *cmds, t_shell *shell)
{
	int	i;

	if (!cmds->argv[1])
		// print env in asciibetical order
		// return 0

	i = 1;
	// while (cmds->argv[i])
		// if !valid_syntax
			// return 0

		// if (key == match)
			//if existing key
				//update existing key-value pair
		// else
			// add to the end of env new key-value pair

	return (0);
}